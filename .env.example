# =========================
# Database (Neon / Postgres)
# =========================

# ASYNC URL (FastAPI / asyncpg)
# Provide via environment variable (GitHub Actions, Render, Docker secrets)
ASYNC_DATABASE_URL=${ASYNC_DATABASE_URL}

# SYNC URL (Alembic / psycopg2)
DATABASE_URL=${DATABASE_URL}

# =========================
# App / Environment
# =========================
ENVIRONMENT=production        # Options: development | staging | production | ci
DEBUG=False                   # Always False in production
SECRET_KEY=${SECRET_KEY}      # Must be securely stored
ALLOWED_ORIGINS=${ALLOWED_ORIGINS}  # Comma-separated list, e.g., https://yourdomain.com

# =========================
# Firebase / Google service account
# =========================
# Use secrets injection (do NOT commit JSON to GitHub)
FIREBASE_CREDENTIALS_JSON=${FIREBASE_CREDENTIALS_JSON}

# For local development only (do not commit)
# FIREBASE_CREDENTIALS_PATH=G:\pycharm\bloodonal\serviceAccountKey.json

# =========================
# Redis
# =========================
REDIS_URL=${REDIS_URL}        # e.g., redis://localhost:6379/0

# =========================
# API
# =========================
API_VERSION=v1
